# ゼロ円で公開するまでの手順

用意するもの: GitHubアカウントとGoogleアカウントだけ。費用は0円です。
できあがるもの: `https://あなたのユーザー名.github.io` で公開される、週2回自動更新のブログ。

所要時間は約1時間（SNSデータのアップロードを除く）。

---

## ⚠ 先に読んでください（重要）

このリポジトリは **Public（公開）** にします。GitHubの無料枠で公開サイトを持つための条件です。
つまり **アップロードしたファイルは誰でも見られます。**

SNSのエクスポートには、投稿本文だけでなく **DM・メールアドレス・電話番号・広告データ** も含まれています。
**丸ごとアップロードしないでください。** 必要なのは投稿本文のファイルだけです。

### アップロードするファイル（これだけ）

| 媒体 | ファイル |
| --- | --- |
| X | `data/tweets.js`（`tweets-part1.js` などがあればそれも） |
| Instagram | `your_instagram_activity/media/posts_1.json`（posts_2, posts_3... があれば全部） |
| Facebook | `your_facebook_activity/posts/your_posts__check_ins__photos_and_videos_1.json` |

### 絶対にアップロードしないもの

`direct-messages.js` / `messages` フォルダ / `account.js` / `personal_information` /
`ads_information` / `connections` / 写真・動画ファイル

投稿本文はもともと公開しているものなので問題ありません。それ以外は不要です。

---

## 手順1: GitHubアカウントを作る

github.com → Sign up。ユーザー名はサイトのURLになるので、短く読みやすいものを選んでください。

---

## 手順2: リポジトリを作る（名前がとても重要）

1. 右上の「+」→ **New repository**
2. **Repository name** に `あなたのユーザー名.github.io` と入力
   - 例: ユーザー名が `terashi2026` なら `terashi2026.github.io`
   - **この名前にする理由**: サイトがURLの直下に置かれます。別の名前だと
     `.../リポジトリ名/` という階層ができ、あとで独自ドメインに移すときに
     設定を何箇所も直すことになります。
3. **Public** を選択
4. 「Add a README file」にチェック
5. **Create repository**

---

## 手順3: ファイルを入れる

1. `blog-starter.zip` を解凍する
2. リポジトリのページで **Add file** → **Upload files**
3. 解凍してできた `blog` フォルダの **中身をすべて** ドラッグ&ドロップ
   （`blog` フォルダごとではなく、その中身です）
4. 下までスクロールして **Commit changes**

### つまずきやすい点

`.github` という**ドット始まりのフォルダ**が一緒に入っている必要があります。
Macでは隠しフォルダ扱いなので、Finderで `Cmd + Shift + .` を押すと表示されます。
Windowsは エクスプローラーの「表示」→「隠しファイル」にチェック。

アップロード後、リポジトリのトップに `.github`, `src`, `scripts`, `data`,
`keywords.txt`, `package.json` が並んでいればOKです。

---

## 手順4: GitHub Pages を有効にする

1. **Settings** タブ → 左メニュー **Pages**
2. Build and deployment の Source を **GitHub Actions** に変更

保存ボタンはありません。選んだ時点で反映されます。

---

## 手順5: Actionsの権限を設定する

1. **Settings** → 左メニュー **Actions** → **General**
2. 一番下の「Workflow permissions」で
   - **Read and write permissions** を選ぶ
   - **Allow GitHub Actions to create and approve pull requests** にチェック
3. **Save**

※ここを飛ばすと、記事はできてもプルリクエストが作られずエラーになります。

---

## 手順6: サイトが出るか確認する

1. **Actions** タブを開く
2. 「サイトを公開」が動いているはずです（1〜2分）
3. 緑のチェックがついたら `https://あなたのユーザー名.github.io` を開く

サンプル記事が1本入った状態のサイトが表示されれば成功です。

---

## 手順7: Gemini APIキーを登録する

1. aistudio.google.com にGoogleアカウントでログイン
2. **Get API key** → **Create API key** → 表示されたキーをコピー
   （無料枠のまま使います。課金設定はしないでください）
3. リポジトリの **Settings** → **Secrets and variables** → **Actions**
4. **New repository secret**
   - Name: `GEMINI_API_KEY`
   - Secret: コピーしたキー
5. **Add secret**

---

## 手順8: SNSのデータを入れる

上の「⚠ 先に読んでください」で指定したファイルだけを用意します。

1. リポジトリの `data` フォルダ → `raw` フォルダを開く
2. **Add file** → **Upload files**
3. 対象のJSONファイルをドラッグ&ドロップ
4. **Commit changes**

1回のアップロードは100ファイルまでです。多い場合は何回かに分けてください。
フォルダ構成は気にしなくて大丈夫です（中を自動で探しに行きます）。

---

## 手順9: 記事を1本作ってみる

1. **Actions** タブ → 左の「記事の自動生成」
2. 右の **Run workflow** → **Run workflow**
3. 1〜2分待つ
4. **Pull requests** タブに記事が届く

中身を読んで、問題なければ **Merge pull request**。
Mergeすると「サイトを公開」が自動で走り、1分ほどでサイトに反映されます。

### 記事ができなかったとき

Actionsのログに「体験が◯件しかありません」と出ていたら、
`keywords.txt` の3列目「関連語」が、実際の投稿で使っている言葉とズレています。
自分がSNSでよく書く言い回しに直してください。

---

## 手順10: サイト名などを自分のものにする

リポジトリの `src/site.ts` を開き、右上の鉛筆アイコンで編集します。

- `title`: サイト名
- `tagline`: 短い説明
- `description`: トップページに出る文章
- `author`: 運営者名（プライバシーポリシーにも使われます）

編集して Commit changes を押せば、自動で再公開されます。

---

## 手順11: アドセンス申請の準備（無料でできる範囲）

| 項目 | 状態 |
| --- | --- |
| プライバシーポリシー | 用意済み（`/privacy/`） |
| 運営者情報 | 用意済み（`/about/`）※ `src/site.ts` の名前を直す |
| お問い合わせ | **要作業**（下記） |
| sitemap.xml / RSS | 自動生成済み |
| 記事15〜20本 | 週2本ペースで約2ヶ月 |

### お問い合わせフォームの作り方

1. Googleフォームで新規フォーム作成（名前・メール・内容の3項目でOK）
2. 右上の **送信** → **< >** タブ → HTMLをコピー
3. リポジトリの `src/pages/contact.astro` を開いて編集
4. `（ここに Google フォームを埋め込みます）` と書かれた `<p>` を消して、コピーしたHTMLを貼る
5. Commit changes

---

## 独自ドメインを買ったあとにやること（3箇所だけ）

無料版で作ったものは、そのまま引っ越せます。

1. ドメインを買う（Cloudflare Registrar が原価販売で安い）
2. **Settings** → **Pages** → Custom domain に買ったドメインを入力
3. `.github/workflows/deploy.yml` の `SITE_URL` の行を
   `https://買ったドメイン` に書き換える
4. ドメイン側のDNSに、GitHubが指示するレコードを設定

その後、adsense.google.com から申請します。

---

## 調整できる場所

| やりたいこと | 変更する場所 |
| --- | --- |
| 更新頻度を変える | `.github/workflows/publish.yml` の cron |
| 確認なしで全自動にする | 同ファイルの `DIRECT_PUSH: "true"` |
| 記事のテーマを増やす | `keywords.txt` に行を追加 |
| 文体・構成を変える | `scripts/generate.mjs` のプロンプト部分 |
| 配色・デザイン | `src/styles/global.css` の一番上の色定義 |

---

## つまずいたときは

| 症状 | 原因と対処 |
| --- | --- |
| Actionsが赤くなる | ログの赤い行を読むと原因が書いてあります。多くは手順5の権限設定漏れ |
| サイトが404 | 手順4のSourceが「GitHub Actions」になっているか確認 |
| 日本語が文字化け | エクスポートでHTML形式を選んでいます。JSONで取り直してください |
| 429エラー | Geminiの無料枠上限。時間をおけば自動で再試行されます |
| 記事の内容が事実と違う | プルリクエストの Files changed から直接編集できます |
